$(function(){
   // var socket = io.connect("http://localhost:82");
    var chatkey ="";
    var receiveId ="";
    var sendId = "";
    
});

function viewChat(dm_id,dm_key){
	alert(dm_id+","+dm_key);
}